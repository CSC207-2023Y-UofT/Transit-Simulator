package entity.model.train;

/**
 * The TrainRole enum represents the different jobs that a train can have.
 */
public enum TrainRole {
    /**
     * The OPERATOR role represents an employee who operates the train.
     */
    OPERATOR,

    /**
     * The ENGINEER role represents an employee who maintains the train.
     */
    ENGINEER
}
